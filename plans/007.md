# Plan 007 — Final Report + Polish

## Goal
Generate a comprehensive final report with a per-skill breakdown and improvement roadmap, and apply UI polish across the app.

## Prerequisites
Plan 006 Done.

## Drift-Check
Run this command to verify the repo is in the expected state:
```powershell
if (Test-Path "client/src/pages/FinalReport.jsx") { Write-Output "FinalReport already exists" } else { Write-Output "Ready" }
```

## Steps

### Step Group 1: Server Report Generation
**Files touched:** `server/src/routes/session.js`
**What to do:** Implement `GET /api/session/:id/report`. Aggregate session data into a report:
- Overall score (average of all answer scores * 20)
- Per-skill confidence
- Strengths (skills with confidence >= 4)
- Weaknesses (skills with confidence <= 2)
- Improvement roadmap (Gemini-generated based on weak skills)
**Verification:** API returns comprehensive report JSON.

### Step Group 2: Client Final Report Page
**Files touched:** `client/src/pages/FinalReport.jsx`
**What to do:** Build FinalReport page:
- Hero score ring
- Tabs: Overview / Skills / Strengths & Weaknesses / Roadmap
- Skill badge grid
- Accordion cards per skill
- Export to PDF button (`window.print()`)
**Verification:** Page renders correctly with all tabs and exports to PDF.

### Step Group 3: UI Polish
**Files touched:** `client/src/styles/*`, `client/src/App.jsx`
**What to do:** Apply full `DESIGN.md` design system across all pages (Inter font, color palette, animations). Add loading states, error boundaries, empty states, and ensure a mobile-responsive layout.
**Verification:** App looks polished and adheres strictly to DESIGN.md.

## Commit
Message format: 
- `007: implement server report generation`
- `007: build client final report page`
- `007: apply full ui polish`

## Verification
Navigate through the entire app, ensure design consistency, responsiveness, and view a completed final report.
